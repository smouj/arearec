# Third-party software

## FFmpeg

AreaRec can invoke an FFmpeg executable supplied by the user or bundled by a distributor. FFmpeg is a separate project. Its exact licensing obligations depend on how that FFmpeg binary was built (for example LGPL/GPL configuration and linked codecs).

This repository does not redistribute FFmpeg binaries.
